# تصميم محرك الزمن المستمر

## الغاية

يستبدل هذا التصميم استعمال **رقم الدور** كوحدة للزمن بساعة داخلية متصلة. يظل رقم الدور معرفًا تقنيًا للمشهد ولضمان عدم تكرار المعاملات، لكنه لا يثبت اليوم أو ساعة الوصول أو موعد الضريبة أو إتاحة الشخصية. المرجع الزمني القانوني هو `absolute_minutes` في جدول `world_clock`.

> **قاعدة العالم:** لا يُفترض أن كل قرار يساوي يومًا. يقدر النظام مدة الفعل الواقعية بالدقائق، ولا ينتقل إلى فترة طويلة إلا عندما يطلب اللاعب الانتظار أو القفز إلى موعد محدد، أو يكون الفعل نفسه رحلة أو معركة أو عملًا يستغرق وقتًا معلومًا.

## نموذج الساعة

| الحقل | الغرض |
|---|---|
| `absolute_minutes` | الطابع الزمني الحتمي منذ بداية الحملة؛ وهو مرجع الرحلات والتسويات والمواعيد. |
| `day`, `month`, `year`, `season` | عرض تقويمي مشتق ومتوافق مع الواجهة القديمة. |
| `hour`, `minute` | وقت اليوم المعروض للاعب. |
| `day_minutes` | عدد دقائق اليوم، وهو 1,440. |

يستخدم التقويم 30 يومًا في الشهر و12 شهرًا في السنة. تبدأ الحملة في اليوم الأول عند الساعة 08:00. لا يُستخدم الزمن الحقيقي لنظام التشغيل أو ساعة المتصفح؛ فالزمن مستقل وحتمي وقابل لإعادة الإنتاج داخل الحملة.

## تحديد المدة

| حالة الفعل | المدة التي يحددها النظام تلقائيًا |
|---|---:|
| حوار، تفقد، سؤال، قرار إداري بسيط | 10 دقائق |
| تفاوض، بحث، ترتيب، تدريب، تحقيق | 30 دقيقة |
| مناوشة أو حراسة أو عمل ميداني | 60 دقيقة |
| رحلة داخل الإقليم | 6 ساعات |
| رحلة بين إقليمين | 18 ساعة |
| معركة كبرى أو حصار أو حدث يصرح بساعاته | المدة المحددة أو 8 ساعات عند الغموض |

تعمل هذه القواعد قبل الاستدلال اللغوي العام: إذا كتب اللاعب مدة صريحة مثل «انتظر ساعتين» أو «اقفز إلى الفجر» أو «بعد ثلاثة أيام»، تُحلل المدة أو الموعد أولًا وتُطبق القيمة الصريحة بعد التحقق. لا تسمح الواجهة بقفز إلى زمن ماضٍ.

## الرحلات والحضور

تستخدم جداول الحضور والحركة حقولًا جديدة في الدقائق: `available_at_minute` و`depart_at_minute` و`arrival_at_minute`. تبقى الحقول القديمة (`available_turn` و`depart_turn` و`arrival_turn`) للترحيل والقراءة التوافقية فقط؛ ولا تُستخدم لقرار الإتاحة بعد اكتمال الهجرة.

عند اعتماد حركة، تسجل الشخصية في حالة `traveling` حتى تصل الساعة إلى `arrival_at_minute`. عندها فقط تنقل تلقائيًا إلى وجهتها وتصبح `active`. لا يصبح ذكر «الدور التالي» أو حفظ مشهد جديد سببًا للوصول.

## التسويات المدنية والاقتصادية

تنتقل التسوية من «مرة لكل دور» إلى «مرة لكل يوم داخل العالم». تحسب `settle_civic_time(current_minute)` عدد أيام العالم المكتملة منذ آخر تسوية، ثم تسوي كل يوم متأخر مرة واحدة وبمفتاح منع تكرار مبني على رقم اليوم العالمي. أرباح المتاجر والضرائب والمخزون لا تتغير حين يدور اللاعب في عشر محادثات متتابعة داخل عشر دقائق، لكنها تتغير إذا مر يوم أو أكثر فعليًا بسبب نشاط أو انتظار أو رحلة.

## الترحيل والتوافق

تضاف الحقول الجديدة دون حذف بيانات الحملة. تُشتق قيمة البداية من التاريخ القائم: `(اليوم - 1) × 1440 + 480`. تُحول رحلات قيد التنفيذ القديمة إلى موعد وصول محافظ وفق موقعها وحالتها، ثم تصبح كل الرحلات الجديدة زمنية بالدقائق. يحتفظ `turn_number` للمشاهد ودفتر القيود وسجل التدقيق فقط.

## معايير القبول

يقبل التنفيذ عندما تحقق الحملة الشروط الآتية: لا يزيد زمن العالم يومًا لمجرد وجود `world_changes` أو مشهد جديد؛ تتقدم المحادثة وفق مدة قصيرة محسوبة؛ يحقق الانتظار أو القفز الصريح المدة المطلوبة؛ لا تصل الشخصية المسافرة قبل `arrival_at_minute`؛ وتصبح تسوية المدن مرة لكل يوم داخلي لا مرة لكل مشهد.
