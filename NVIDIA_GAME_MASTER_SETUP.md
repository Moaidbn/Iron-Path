# NVIDIA Nemotron Game Master

تمت إعادة تشكيل **Game Master** في «سجل طريق الحديد» ليعمل بوصفه طبقة سردية خارجية اختيارية مبنية على NVIDIA NIM ونموذج `nvidia/nemotron-3-super-120b-a12b`.[1]

> **المبدأ الحاكم:** النموذج يكتب السرد والحوار والاقتراحات فقط. أما المال، المتاجر، الحضور، مواقع الشخصيات، حركات السفر، الجيوش، المهام، المخزون، والوقت فتظل تحت سيطرة محرك الحالة المحلي الحتمي.

## إعداد المفتاح

انسخ القالب ثم ضع مفتاحك في النسخة المحلية فقط:

```bash
cd iron_path_reforged
cp .env.example .env
```

حرر `.env` وضع مفتاح NVIDIA في المتغير التالي:

```dotenv
NVIDIA_API_KEY=nvapi-...
```

يبقى `.env` خارج Git وحزمة التسليم عبر ملف `.gitignore`. لا تضف المفتاح إلى `app.py` أو ملفات HTML أو قاعدة البيانات.

## الإعدادات الافتراضية

| المتغير | القيمة الافتراضية | الغرض |
|---|---|---|
| `NVIDIA_GM_ENABLED` | `true` | تشغيل أو إيقاف الراوي الخارجي |
| `NVIDIA_API_BASE` | `https://integrate.api.nvidia.com/v1/chat/completions` | نقطة NVIDIA NIM المتوافقة مع OpenAI |
| `NVIDIA_API_MODEL` | `nvidia/nemotron-3-super-120b-a12b` | نموذج الراوي |
| `NVIDIA_GM_CONTEXT_MODE` | `compact` | `compact` أسرع ومناسب للعب؛ `full` أبطأ وأعمق للمشاهد المهمة |
| `NVIDIA_GM_TIMEOUT_SECONDS` | `60` | حد انتظار الاستجابة؛ لا يزيد سرعة النموذج، بل يمنع fallback المبكر |
| `NVIDIA_GM_MAX_TOKENS` | `900` | سقف طول المشهد؛ 900 متوازن، و1400 للمشهد العميق |
| `NVIDIA_GM_ENABLE_THINKING` | `false` | تعطيل التفكير المطوّل لتقليل زمن الدور |
| `NVIDIA_GM_LOG_TIMING` | `false` | عند `true` يسجل الخادم زمن الطلب وحجمه التشغيلي دون المفتاح |

## التشغيل والتحقق

```bash
PORT=5011 python3 app.py
curl http://127.0.0.1:5011/health
```

عند نجاح الإعداد سيعيد `/health` حقل `engine` بالقيمة `nvidia-nemotron`. بعد تنفيذ فعل واحد، يعرض أيضًا `narration_status=ready` و`narration_latency_ms` وزمن آخر طلب، إضافة إلى `narration_context_mode`. عند غياب المفتاح أو تعذر NVIDIA، يعود المحرك تلقائيًا إلى `local` ويستمر اللعب بالنص المحلي بدل توقف الحملة.

## ضبط السرعة والطول

الإعداد الموصى به للعب اليومي هو:

```dotenv
NVIDIA_GM_CONTEXT_MODE=compact
NVIDIA_GM_MAX_TOKENS=900
NVIDIA_GM_TIMEOUT_SECONDS=60
NVIDIA_GM_ENABLE_THINKING=false
```

هذا لا يجعل نموذج 120B مساويًا لسرعة نموذج Flash أصغر، لكنه يقلل الحمولة ويرفع طول السرد عن إعداد `650` السابق. للمشهد المهم فقط استخدم:

```dotenv
NVIDIA_GM_CONTEXT_MODE=full
NVIDIA_GM_MAX_TOKENS=1400
```

ثم أعد الخادم إلى `compact` بعد المشهد؛ وضع `full` يرسل عددًا أكبر من الفصائل والشخصيات والمهام والمشاهد السابقة، لذلك هو أبطأ. ولقياس الزمن الحقيقي مؤقتًا:

```dotenv
NVIDIA_GM_LOG_TIMING=true
```

بعد تنفيذ فعل، راقب نافذة الخادم أو نفّذ:

```powershell
curl.exe http://127.0.0.1:5011/health
```

## تسلسل الجولة

1. يتحقق الخادم المحلي من الفعل، الحضور، السفر، الوقت، والاقتصاد.
2. يحسب الخادم النتيجة الدائمة ويجهز حقائق موثوقة ومحدودة الحجم.
3. يرسل الراوي هذه الحقائق وسجلًا قصيرًا للسياق إلى NVIDIA.
4. يقبل النص فقط إن كان JSON سليمًا وباللغة المطلوبة.
5. تُحفظ النتيجة السردية، ثم تُطبق تغييرات الحالة الحتمية نفسها.
6. عند خطأ الشبكة أو انتهاء المهلة أو خروج بلغة خاطئة، يستخدم النص المحلي تلقائيًا.

## اللغة والسلامة

- اختيار الواجهة `ar` يطلب سردًا عربيًا. صفحة NVIDIA الرسمية لا تذكر العربية ضمن اللغات المدعومة المعلنة لـNemotron 3 Super، لذلك قد تكون جودة العربية أو ثباتها أقل من الإنجليزية؛ يمكن إبقاء المحرك المحلي كـfallback عند مخالفة اللغة.
- اختيار الواجهة `ar` يفرض سردًا عربيًا.
- اختيار الواجهة `en` يرفض أي رد يحتوي على أحرف عربية ويعود محليًا عند عدم الالتزام.
- لا يتلقى الطلب مفتاح API أو ملفات المستخدم أو أي تعليمات غير موثوقة من الخارج.
- لا يسمح العقد للنموذج بابتكار أموال أو تغييرات دائمة أو حضور شخصية غير موثق.

## المراجع

[1]: https://docs.api.nvidia.com/nim/reference/nvidia-nemotron-3-super-120b-a12b "NVIDIA Nemotron 3 Super 120B A12B API Reference"
[2]: https://docs.api.nvidia.com/nim/docs/api-quickstart "NVIDIA NIM API Quickstart"
