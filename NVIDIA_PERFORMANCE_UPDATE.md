# تحديث أداء NVIDIA Game Master

## النتيجة

يعالج هذا التحديث مشكلتين منفصلتين: **الانتظار الطويل** و**قِصر السرد بعد الانتظار**. لا يمكن لنموذج Nemotron 3 Super 120B أن يطابق زمنيًا نموذج Flash صغيرًا عند طلب متزامن واحد؛ لذلك خُفف السياق الافتراضي، وعُطل التفكير المطول، وأضيفت قياسات شفافة للطلب. كما لم يعد التطبيق يقبل مشهدًا قصيرًا جدًا بعد انتظار طويل؛ عند مخالفة الحد يرجع إلى السرد المحلي الآمن بدل عرض رد مختصر غير مُرضٍ.

| التحسين | التطبيق | الأثر |
|---|---|---|
| وضع سياق افتراضي `compact` | يقلل عدد الفصائل والمرافقين والمشاهد والسجلات المرسلة | طلب أخفّ مع بقاء الحقائق المهمة |
| حد سرد أدنى | 650 حرفًا في الوضع المختصر و900 في الوضع العميق | منع قبول مشاهد مقتضبة جدًا |
| تعطيل التفكير | `NVIDIA_GM_ENABLE_THINKING=false` | لا تُهدر ميزانية الإخراج في التفكير المطول |
| تشخيص صحي | `/health` يعرض الزمن والحجم وسبب الإنهاء | يوضح أين يحدث البطء أو القطع |
| مؤشر انتظار | الواجهة تعرض «الراوي يكتب المشهد…» والوقت المنقضي | تحسن تجربة اللعب حتى أثناء انتظار الطلب |

> تظل كل القرارات الدائمة، والمال، والحضور، والمواقع، والمهام تحت تحكم المحرك المحلي. NVIDIA يكتب السرد فقط ولا يفرض حالة جديدة على الحملة.

## الإعداد الموصى به للعب اليومي على Windows

ضع هذه القيم في `C:\Users\medom\desktop\AI_RPG\.env` ثم أعد تشغيل `python app.py`:

```env
NVIDIA_GM_CONTEXT_MODE=compact
NVIDIA_GM_ENABLE_THINKING=false
NVIDIA_GM_MAX_TOKENS=900
NVIDIA_GM_TIMEOUT_SECONDS=60
NVIDIA_GM_LOG_TIMING=true
```

بعد أول طلب ناجح، افتح:

```powershell
curl.exe http://127.0.0.1:5011/health
```

وستجد حقولًا غير حساسة مشابهة لما يلي:

```json
{
  "engine": "nvidia-nemotron",
  "narration_status": "ready",
  "narration_context_mode": "compact",
  "narration_latency_ms": 18400,
  "narration_finish_reason": "stop",
  "narration_prompt_chars": 4700
}
```

بعد قياس عدة أدوار، أعد `NVIDIA_GM_LOG_TIMING` إلى `false` لتقليل ضوضاء سجل الخادم. إذا كانت `narration_finish_reason` تساوي `length`، ارفع `NVIDIA_GM_MAX_TOKENS` تدريجيًا إلى `1100` بدل رفعه دفعة واحدة إلى الحد الأقصى.

## وضع المشاهد المهمة

عند محادثة ملك، قرار حرب، أو حدث مفصلي، يمكن استخدام سياق أعمق مؤقتًا:

```env
NVIDIA_GM_CONTEXT_MODE=full
NVIDIA_GM_MAX_TOKENS=1400
NVIDIA_GM_TIMEOUT_SECONDS=120
```

أعد القيم إلى وضع اللعب اليومي بعد المشهد، لأن `full` يرسل سياقًا أكبر ويستغرق وقتًا أطول عمدًا.

## عند الحاجة إلى سرعة أقرب إلى Flash

التحسين أعلاه يخفض حجم الطلب ولا يغيّر حجم نموذج Nemotron نفسه. للمحادثات السريعة المتكررة، اختبر نموذجًا أصغر في حساب NVIDIA الخاص بك، مثل `nvidia/nemotron-3-nano-30b-a3b` إذا كان متاحًا لديك، عبر تغيير قيمة `NVIDIA_API_MODEL` فقط ثم اختبار `/health` وفعل داخل اللعبة. لا تنتقل إليه نهائيًا قبل التأكد من التزامه بعقد JSON واللغة المختارة؛ أبقِ نموذج 120B للمشاهد المهمة إذا كانت جودة السرد هي الأولوية.[1]

## التحقق قبل الاستبدال

اختبارات محلية من دون أي مفتاح أو اتصال خارجي أكدت ما يلي:

| الاختبار | النتيجة |
|---|---|
| صحة ملفات Python المعدلة | ناجحة |
| مقارنة حجم طلب `compact` و`full` | 4,638 مقابل 6,483 حرفًا في العينة |
| قبول JSON طويل صالح | ناجح، مع `finish_reason=stop` |
| عرض حقول التشخيص في `/health` | ناجح |
| ظهور مؤشر الانتظار المترجم في الواجهة | ناجح |

## طريقة تثبيت التحديث

استخرج الحزمة فوق مجلد اللعبة أو انسخ الملفات المعدلة التالية إلى `C:\Users\medom\desktop\AI_RPG`:

```text
nvidia_gm.py
offline_engine.py
app.py
templates\index.html
.env.example
NVIDIA_GAME_MASTER_SETUP.md
NVIDIA_PERFORMANCE_UPDATE.md
NVIDIA_MODEL_PERFORMANCE_NOTES.md
```

**لا تستبدل ملف `.env` الخاص بك**؛ فهو يحتوي مفتاح NVIDIA الحقيقي. بعد النسخ أعد تشغيل الخادم، أرسل فعلًا واحدًا، ثم راجع `/health`.

## المراجع

[1]: https://build.nvidia.com/models "NVIDIA Build – Models Catalog"
